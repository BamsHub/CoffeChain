import Base64Page from '@/sections/base64/Base64Page';

export const metadata = {
    title: 'Base64 Tool — CoffeeChain',
    description: 'Encode & decode Base64 secara otomatis. Tool gratis dari CoffeeChain.',
};

export default function Base64Route() {
    return <Base64Page />;
}
